const allCards = document.querySelectorAll(".peoples-card")
const cardsContainer = document.querySelector(".hero_img-container--cards-container")


function startScrolling () {
    console.log(allCards, cardsContainer)

    // while (true) {
    //     allCards.forEach(card => {
    //         card.style.left += 1;
    //     })
    // }
}

export default startScrolling